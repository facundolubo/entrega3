function Dashboard () {
    return (
        <div>
            <h1>Dashboard</h1>
            <p> Esta es la página de dashboard que se tiene que direccionar siempre. 
                No tiene que ser index.html </p>        
            </div>
    );
}

export default Dashboard;